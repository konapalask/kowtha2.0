version: '3.8'
services:
  db:
    image: postgres:14
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: loan_verification
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
